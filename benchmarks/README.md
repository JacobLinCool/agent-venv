# Benchmarks

Reserved for future cross-language performance comparisons. Empty in v0.

When populated, expected layout:

```
benchmarks/
├── cases/         # benchmark scenarios as JSON
├── runners/       # one per language
└── reports/       # generated, gitignored
```

The conformance protocol can be reused as a benchmarking transport.
